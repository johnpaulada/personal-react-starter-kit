import styled from "styled-components"
import FullContainer from "./FullContainer"

const CenteredFullContainer = styled(FullContainer)`
  justify-content: center;
  align-items: center;
`

export default CenteredFullContainer
